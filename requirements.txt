# AI-Investment-Architectures — pinned runtime dependencies.
# All data sources are free/keyless; these are just client libraries.
requests>=2.31,<3
PyYAML>=6.0,<7
numpy>=1.26,<3
pandas>=2.0,<3
yfinance>=0.2.40        # primary price source (with keyless fallbacks if it breaks)
python-pptx>=0.6,<1     # deck-updater skill
reportlab>=4.0          # branded PDF report output (pure-python)
trafilatura>=1.8        # keyless article-body extraction (news / web-search --full)

# --- Optional data-source libs (all LAZY-imported; modules degrade to empty if absent).
#     Treasury / Damodaran / FRED / BLS / ECB / World Bank / CFTC / segments / ownership /
#     sanctions reach their sources via plain requests — no library needed.
finvizfinance>=0.14      # Finviz key stats + screener (finviz.py)
duckdb>=0.10             # SEC DERA bulk num/tag/sub cross-company queries (bulk.py)
pytrends>=4.9            # Google Trends sentiment overlay (altdata.py)
# praw>=7                # Reddit alt-data (altdata.py) — needs free Reddit API keys
